Overall, our group used three different pretrained models from HuggingFace and one logistic regression (with feature engineering) to complete the song classification task. To improve, experiment with different types of feature beyond engineering, along with more experiment with the hyperparameters for the pretrained models to potentially improve performance. We compared our approaches along with the different submissions done by our classmates and realized the major challenges that came with classifying song genres with lyrics alone, since genres are defined beyond just the lyrics of a song (instruments, tone of the song, etc.). 